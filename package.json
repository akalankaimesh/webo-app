{
  "name": "webo-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Monorepo root for WeBo frontend and backend",
  "scripts": {
    "dev:web": "npm --prefix webo-app run dev",
    "dev:api": "npm --prefix backend-server run dev",
    "dev": "concurrently -k -n web,api -c cyan,green \"npm run dev:web\" \"npm run dev:api\"",
    "start:web": "npm --prefix webo-app run start",
    "start:api": "npm --prefix backend-server run start",
    "start": "concurrently -k -n web,api -c cyan,green \"npm run start:web\" \"npm run start:api\"",
    "lint:web": "npm --prefix webo-app run lint"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
